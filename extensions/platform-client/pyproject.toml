[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "platform-client"
version = "0.1.0"
description = "Shared scaffold for platform clients: the capability-independent parts of an extension MCP server."
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.0,<2",
    "websockets>=12",
]

[project.optional-dependencies]
dev = ["pytest>=8", "ruff>=0.6"]

[project.scripts]
platform-client = "platform_client.__main__:main"

[tool.setuptools.packages.find]
where = ["src"]
